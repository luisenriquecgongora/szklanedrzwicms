{
  "routes": [
    {
      "method": "GET",
      "path": "/office-images",
      "handler": "office-images.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/office-images/count",
      "handler": "office-images.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/office-images/:id",
      "handler": "office-images.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/office-images",
      "handler": "office-images.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/office-images/:id",
      "handler": "office-images.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/office-images/:id",
      "handler": "office-images.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
